const typed = new Typed(".auto-type", {
  strings: ["Developer", "FreeLancer", "Tester"],
  typeSpeed: 100,
  backSpeed: 50,
  loop: true,
  backDelay: 1000,
  cursorChar: "|",
  smartBackspace: true,
});
